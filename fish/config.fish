# Aliases
alias bat batcat
alias cat batcat
alias cp "cp -iv"
alias find fdfind
alias grep rg
alias ls exa
alias mkdir "mkdir -v"
alias mv "mv -iv"
alias rm "rm -Iv"

# Welcome message
set fish_greeting

# Git Prompt
set -g GIT_PS1_SHOWDIRTYSTATE 1
set -g GIT_PS1_SHOWSTASHSTATE 1
set -g GIT_PS1_SHOWUNTRACKEDFILES 1
